import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    feed: []
}

const feedSlice = createSlice({
    name: 'feed',
    initialState: [],
    reducers: {
        
    },
    extraReducers: (builder) => {
        
    }
})